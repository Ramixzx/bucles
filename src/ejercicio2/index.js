// Se le da un inicio y un final al bucle con estas variables.
let inicio = 7;
let final = 12;

// Loop con for
for(inicio; inicio <= final; inicio++){
    console.log(`Vueltita numero ${inicio}`);
};

// Loop con while
while (inicio <= final) {
    console.log(`Vuelta numero ${inicio}`);
    inicio++;
};
