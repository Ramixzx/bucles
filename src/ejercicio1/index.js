let vuelta = 1;
let limite = 17;

while (vuelta <= limite) {
    console.log(`Vuelta numero ${vuelta}`);
    vuelta++;
};

for(vuelta; vuelta <= limite; vuelta++){
    console.log(`Vueltita numero ${vuelta}`);
};